> 本篇会基于 C 语言的视角讨论局部变量。  
> 由于与程序设计的思想比较强相关，恕我无法给出能够规避它的答案。

### 基本概念
局部变量和程序中的 local variable 作用雷同，它的生命周期是一整局游戏，类型是`bool`，有两个值，0或1。

```c
#define bool char
bool psyb_destroyed = 0;
bool ally_transports_alive = 1;
```

> ~~而射哥同样地把它扩展为了`int`，或者至少是`short`。~~

### 初始化、赋值
在**编辑→局部变量**里，你可以选中下拉列表最后一个`No Name`，填上自己的变量名，这叫做**声明**：
```cpp
bool ssks;
```

而同样在那个局部变量窗口，你可以选中已有的变量设初值，这就叫**初始化**：
```cpp
bool ssks = 0;
```

而在变量已经声明的前提下，其他任何（经由触发和动作脚本）对变量进行再次修改的操作，称为**赋值**。  

触发行为中则有两个行为来实现赋值操作，分别是**设置局部变量**和**清除局部变量**，分别相当于将指定的局部变量置为`1`和`0`。  
地图开发过程中，往往将一个特定触发的行为加上设置局部变量，用以标记某个行为已经发生。

### 基本操作：判断变量值
前面说到，地图的局部变量实际上是一种`bool`变量，则可被判断的特性是局部变量设计的核心。

触发中有两个条件`36 局部变量被设置（值为 1）`和`37 局部变量被清除（值为 0）`，可以在触发流程中判定局部变量的值，间接判断某些事情是否已经达成：
- 小队到达目标位置
- 该局游戏已经超过了指定时间
- 多个目标都被摧毁/安全护送
- ...

> 下面的内容就不适合没学过相关姿势的用户观看了。

### 高阶操作：或、非判断

结合[程序化顺序结构思想](地图资料-程序化顺序结构思想.md)里介绍的逻辑原理，这里进一步讨论引入局部变量后会有什么变化：

我们在入门章节[熟悉触发编辑器](./chapter1-getstarted/trigger-editor.md)那边就讲过了，触发必须在**所有**条件**全部满足**的情况下才会执行，那么不妨把触发的条件看成只能用`&&`（且逻辑）连接起来的表达式：

```c
if (TimeElapsed(114) && TechnoExist("NAPSYB", 1) && ...) {
    // ...
}
```

而我若是想表达「该局游戏尚未超时」，或是「游戏耗时已过……，**或者**心灵信标还在」，那么事情就比较难办了。  
这个时候唯有局部变量可解。

```c
bool bonus_caco_valid = 1;
// trigger: [EstherEgg] time limit
if (GameTimeElapsed(114514)) {
    bonus_caco_valid = 0;
}
// trigger: [EstherEgg] condition check
if (TechnoExist("NAPSYB", 1) && bonus_caco_valid == 1) {
    ShowTextBrief("MISSION:ALL01_SSKS");
}
```

在上面的伪代码中，设了个`valid`变量用于检验「尚未超时」这个条件，这样间接实现了`a && !b`这样的“非”判断；

而若需实现“或”判断，则需要“殊途同归”，分别判断每个条件：
```c
bool bonus_caco = 0;
// trigger: [Egg] 1. time reached
if (GameTimeElapsed(114514) && bonus_caco == 0) {
    bonus_caco = 1;
}
// trigger: [Egg] 2. beacon exists
if (TechnoExist("NAPSYB", 1) && bonus_caco == 0) {
    bonus_caco = 1;
}
// trigger: [Egg] condition check
if (bonus_caco == 1) {
    ShowTextBrief("MISSION:ALL01_SSKS");
}
```
在这段伪代码中，又设了个`bonus_caco`变量，互斥地检验两个条件其中一个是否已经满足（若是，则不需要理会另一个），间接实现`a || b`这种“或”判断。

实际触发截出来会比较啰嗦，请容我再次为“增大理解门槛”致歉。
