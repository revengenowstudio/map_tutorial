# 基本概念
顺序结构是自然存在的一种客观事实，在线性系统中，按时间先后执行的流程我们认为就是顺序结构。

# 地图触发的逻辑原理

**从逻辑上**，我倾向于把整个~~游戏~~战役流程称为阉割版的 C 语言，而触发则是一个个 if 单分支结构。  
而整个战役流程就是顺序执行`main()`函数体的过程。

> 注：下列 C 语言代码块**纯属本人杜撰**，不代表真实情况。  
```c
#define bool char

int main(void) {
   // trigger: [Intro] 1. INIT
   if (TimeElapsed(0.1)) {  // event(s)
       // actions
       ShowTextBrief("MISSION:ALL01_A");
       DisableUserInput();
       PlayEVA("EVA_EstablishBattlefieldControl");
       PlayTheme("MechanicalMan");
   }
   ...
}
```

<!-- > 反正对于我这个练习时长两年半的个人练习生来说，没有任何程序设计语言能比它令人头秃。 -->

# 地图触发的运行原理

> Chloride 我不到，问 ZF 罢。
